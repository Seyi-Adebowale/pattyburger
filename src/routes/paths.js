/**
 * Central route path definitions.
 * Reference these everywhere (links, navigation, redirects) instead of
 * hardcoding strings, so routes can be renamed in one place.
 */
export const PATHS = {
  home: '/',
  menu: '/menu',
  bookings: '/pre-order',
  cart: '/cart',
  contact: '/contact',
  notFound: '*',
}
